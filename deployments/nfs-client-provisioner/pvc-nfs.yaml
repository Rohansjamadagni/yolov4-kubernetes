apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfsauto-1
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
